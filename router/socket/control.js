import Player, { Playlist } from "../../modules/player";
import { Socket } from "socket.io";
/**
 *
 * @param {Socket} socket
 * @param {Player} player
 * @param {Playlist} playlist
 */
const Controller = (socket, player, playlist) => {};

export default Controller;
